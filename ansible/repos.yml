---
- name: Repos setup
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Repos setup
      git:
        repo: "git@github.com:grig-iv/{{ item.repo }}.git"
        dest: "{{ item.dest }}"
        update: no
      loop:
        - { repo: "maps", dest: "~/maps" }
        - { repo: "plato", dest: "~/sources/plato" }

